services:
  - type: web
    name: orion-ai
    env: python
    buildCommand: ""
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false
